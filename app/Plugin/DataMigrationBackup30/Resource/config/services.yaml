services:
  _defaults:
    autowire: true
    autoconfigure: true
    bind:
      $projectDir: '%kernel.project_dir%'

  Plugin\DataMigrationBackup30\:
    resource: '../../*'
    exclude: '../../{Resource,PluginManager.php,composer.json}'
